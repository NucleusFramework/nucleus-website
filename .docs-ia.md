# Nucleus docs IA (live line: see lib/nucleus-version.ts)

Narrative-driven structure inspired by the landing page. Reader path: **why Nucleus → first app → architecture → reference**.

## Versioning

**Procedure for the next minor/major:** see [`.docs-versioning.md`](.docs-versioning.md).

- `content/docs/` — **latest** (currently 3.0), served at `/docs`
- `content/versioned/2.5/` — frozen 2.5 archive, served at `/docs/2.5`
- `content/versioned/2.4/` — frozen 2.4 archive, served at `/docs/2.4`
- `content/versioned/2.3/` — frozen 2.3 archive, served at `/docs/2.3`
- `content/versioned/2.2/` — frozen 2.2 archive, served at `/docs/2.2`
- `content/versioned/2.1/` — frozen 2.1 archive, served at `/docs/2.1`
- `content/versioned/2.0/` — frozen 2.0 archive, served at `/docs/2.0`
- Register each archive in `source.config.ts` (`defineDocs`) and `lib/source.ts` (`DOC_VERSIONS`)
- Scaffold with `npm run snapshot-docs <version>` **before** rewriting latest

## Top-level structure

```
content/docs/
  index.mdx                      ← "Welcome" — what Nucleus is, why it exists
  why-nucleus.mdx                ← The Kotlin desktop story (lifted from landing pitch)

  start/
    meta.json
    install.mdx                  ← Install the CLI / demo
    quickstart.mdx               ← 5 min, build first Tao window
    project-setup.mdx            ← Gradle plugin, repository config
    configuration.mdx            ← The full nucleus { } DSL reference

  concepts/                      ← short conceptual pages
    meta.json
    architecture.mdx             ← Layers: your app / Nucleus / CMP / KMP / JDK
    runtimes.mdx                 ← GraalVM Native Image vs JVM+AOT cache
    modules.mdx                  ← Pick what you need
    threading.mdx                ← Host UI thread, NucleusUiThread, event-loop watchdog (3.0)

  windows/                       ← one native backend (Tao); no backend choice since 3.0
    meta.json
    index.mdx                    ← nucleusApplication + DecoratedWindow / HostedWindow
    window-state.mdx             ← window API v2 clone (dev.nucleusframework.window.tao.v2)
    window-scaffold.mdx          ← WindowScaffold, chrome primitives, materials
    dialogs-popups.mdx           ← dialogs, native popup layers
    overlay-windows.mdx          ← transparent, click-through, always-on-bottom
    context-menu.mdx             ← native / themed context menus
    linux-wayland.mdx            ← native Wayland, canPlaceOnScreen
    hidpi.mdx                    ← per-monitor scaling, LCD text on Windows

  multi-window/                  ← @ExperimentalNucleusApi (3.0)
    meta.json
    index.mdx
    satellites.mdx
    docking.mdx
    tabs.mdx

  input/
    meta.json
    trackpad-gestures.mdx        ← Pan / Scale events, raw gestures
    pen-stylus.mdx
    drag-and-drop.mdx
    pointer-icons.mdx            ← TaoPointerIcons
    accessibility.mdx

  native-content/
    meta.json
    native-views.mdx
    texture-view.mdx
    gpu-render-context.mdx

  toolkits/
    meta.json
    index.mdx                    ← native design systems intro
    macos.mdx / fluent.mdx / yaru.mdx / jewel.mdx / material.mdx

  pdf/                           ← external repo (ComposePdfReader), own version
  webview/                       ← external repo (ComposeNativeWebview), own version

  os/                            ← OS integration cluster
    meta.json
    index.mdx
    notifications.mdx            ← Cross-platform notification overview
    notification-macos.mdx       ← Apple Notifications
    notification-windows.mdx     ← WinRT
    notification-linux.mdx       ← FreeDesktop
    system-tray.mdx              ← Tray API
    tray-menu-dsl.mdx
    tray-app.mdx                 ← Lifecycle for tray-only apps
    global-hotkey.mdx
    media-control.mdx
    dark-mode.mdx                ← was darkmode-detector
    system-color.mdx
    system-info.mdx
    menu-macos.mdx
    spell-check.mdx              ← native spell engines + Compose context-menu integration (2.5)
    freedesktop-icons.mdx

  lifecycle/                     ← App lifecycle / launcher
    meta.json
    index.mdx
    application.mdx              ← nucleusApplication options (3.0)
    single-instance.mdx
    deep-links.mdx
    app-metadata.mdx
    executable-type.mdx
    launcher-macos.mdx
    launcher-windows.mdx
    launcher-linux.mdx
    auto-launch.mdx
    scheduler.mdx
    service-management.mdx       ← service-management-macos
    energy-manager.mdx
    taskbar-progress.mdx

  performance/                   ← Performance & native interop
    meta.json
    index.mdx                    ← The two runtimes pitch
    graalvm/
      meta.json
      index.mdx
      configuration.mdx
      automatic-metadata.mdx
      runtime-bootstrap.mdx
      tasks-ci.mdx
    aot-cache.mdx
    native-access.mdx            ← merged native-access/{index,types,usage}
    native-http.mdx              ← merged native-http + ktor + okhttp adapters
    native-ssl.mdx
    linux-hidpi.mdx              ← deep performance section

  packaging/                     ← Was "Features" + targets
    meta.json
    index.mdx                    ← 16 formats overview
    macos.mdx                    ← targets/macos
    windows.mdx
    linux.mdx
    code-signing.mdx
    trusted-certificates.mdx
    sandboxing.mdx
    auto-update.mdx
    publishing.mdx
    ci-cd.mdx

  compare/
    meta.json
    index.mdx                    ← Why Nucleus vs alternatives (lifted from landing)
    packaging.mdx                ← was comparison/packaging
    electron.mdx                 ← NEW — Electron comparison
    tauri.mdx                    ← NEW — Tauri comparison
    cmp-default.mdx              ← NEW — vs vanilla Compose Multiplatform

  ecosystem/
    meta.json
    index.mdx                    ← JVM ecosystem narrative
    file-dialog.mdx

  migrate/
    meta.json
    from-2.x.en.mdx              ← 2.x → 3.0 breaking changes (dotted slug → explicit .en)
    from-1.x.en.mdx              ← was migration-2.0; dotted slugs need an explicit .en (parser: 'dot' would otherwise treat "x" as a locale)
    from-jb-compose.mdx          ← was migration

  reference/                     ← Reference matter
    meta.json
    gradle-dsl.mdx               ← Full DSL spec
    troubleshooting.mdx          ← NEW

  roadmap.mdx
  changelog.mdx
```

## File naming convention (i18n)

Fumadocs `parser: 'dot'` (default): `{name}.mdx` is the default language (EN), `{name}.fr.mdx` is French. Same `meta.json` for both languages.

If `{name}` itself contains a dot (e.g. `from-1.x`), the default-language file **must** be `{name}.en.mdx`. Otherwise the parser treats the last segment as a locale (`from-1.x.mdx` → locale `x`) and the English page 404s. The French file stays `{name}.fr.mdx`.

## meta.json pattern

```json
{
  "title": "Section title",
  "description": "Short hook for the section",
  "pages": ["index", "page1", "page2"]
}
```

## Page template (per page)

Every page follows the same beat structure:

```mdx
---
title: <Concrete value-led title>
description: <One sentence answering: what does this give me?>
---

<Lede paragraph — 2–3 sentences. State the problem, then state what this module solves.>

## TL;DR
- 3-5 bullets, scannable

## Install
<Gradle coordinate or "comes with nucleus-application">

## Quickstart
<Tiniest possible working snippet>

## How it works
<2-3 paragraphs of mental model — borrow the landing narrative tone>

## Reference
<API surface, organized by use case not by class>

## Troubleshooting / Notes
<Optional — only if real gotchas>
```

## Voice & tone

- **Marketing-tinted**: lift phrasing from the landing (e.g. "Cold start of a binary. Throughput of the JVM."). Each section opening should answer "why care?".
- **Senior dev energy**: no hand-holding on Gradle/Kotlin basics. Assume the reader ships software.
- **Show first**: every concept gets a snippet within the first scroll.
- **Avoid passive voice & hedging**. "Nucleus exposes" not "It may be possible to..."
- **No emojis** in body. Optional admonitions via `<Callout>`.
