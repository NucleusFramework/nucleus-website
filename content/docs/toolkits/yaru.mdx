---
title: Yaru
description: Ubuntu Yaru widgets and themes for Compose Multiplatform, with a Nucleus YaruDecoratedWindow.
---

[yaru-compose-ui](https://github.com/NucleusFramework/yaru-compose-ui) ports
[yaru.dart](https://github.com/ubuntu/yaru.dart) to Compose Multiplatform, with a GNOME-style
`YaruDecoratedWindow` built on Nucleus. See the
[live gallery](https://nucleusframework.github.io/yaru-compose-ui/).

## Add the dependency

```kotlin title="build.gradle.kts"
dependencies {
    // Desktop window; brings yaru and nucleus-application with it
    implementation("dev.nucleusframework.yarucompose:yaru-decorated-window:<yaru-version>")
    // Or the design system alone, for non-JVM targets
    // implementation("dev.nucleusframework.yarucompose:yaru:<yaru-version>")
}
```

Pick the release whose README matches your Nucleus line.

## Open a Yaru window

```kotlin
import dev.nucleusframework.application.nucleusApplication
import dev.nucleusframework.yarucompose.themes.YaruTheme
import dev.nucleusframework.yarucompose.themes.YaruVariant
import dev.nucleusframework.yarucompose.window.YaruDecoratedWindow

fun main() = nucleusApplication {
    YaruDecoratedWindow(onCloseRequest = ::exitApplication, title = "Settings") {
        YaruTheme(isDark = false, variant = YaruVariant.Orange) { App() }
    }
}
```

Put `YaruTheme` inside the window content, so the chrome follows the resolved color scheme and
layout direction, right-to-left included.

## What's next

- [Toolkits](/docs/toolkits): compare the other design systems.
- [Window scaffold](/docs/windows/window-scaffold): full-window layouts without a toolkit.
- [Repo README](https://github.com/NucleusFramework/yaru-compose-ui): widgets and releases.
