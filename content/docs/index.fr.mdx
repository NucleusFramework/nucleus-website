---
title: Bienvenue dans Nucleus 2.0
description: Nucleus est un framework Kotlin pour créer des applications de bureau natives multiplateformes au-dessus de Compose Multiplatform.
---

Nucleus est un framework Kotlin pour créer des applications de bureau natives multiplateformes. Il s'appuie sur Compose Multiplatform et ajoute les décorations de fenêtre natives, l'intégration au système d'exploitation, la signature de code et les installateurs natifs. Vous configurez tout le build via un seul DSL Gradle, et le runtime s'exécute soit comme une image native GraalVM, soit sur la JVM.

## Fonctionnalités

- **Un seul langage pour toutes les plateformes.** Partagez votre code Kotlin avec vos apps Android, iOS et web, avec Compose UI par-dessus.
- **Backend Tao (nouveau en 2.0).** Fenêtrage natif en Rust avec prise en charge de Wayland, du multi-touch et du stylet.
- **Deux runtimes, un seul DSL.** Exécution en image native GraalVM, ou sur la JVM avec un cache AOT (JDK 25+).
- **Plus de 40 modules d'intégration OS.** Notifications, system tray, menus dock, dark mode, hotkeys globaux et barre des tâches, chacun ajouté comme dépendance optionnelle.
- **18 formats d'installateur.** DMG, PKG, MSI, AppX, DEB, RPM, AppImage, Snap et Flatpak entre autres, avec signature de code et notarisation.

Pour construire votre première app, suivez le [quickstart](/docs/start/quickstart).

## Quoi de neuf en 2.0

- **Backend Tao.** Remplace le backend AWT par des fenêtres natives pilotées en Rust. Voir [Backends](/docs/concepts/backends).
- **GraalVM Native Image.** Les métadonnées de reachability de chaque module Nucleus sont fournies et résolues automatiquement.
- **Quatre toolkits natifs.** macOS 26 (Liquid Glass), Fluent, Yaru et Jewel partagent le même `DecoratedWindow` avec un habillage différent.
- **18 formats d'installateur.** Désormais avec AppX/MSIX, Flatpak et Snap, avec publication sur les stores.
- **Cache AOT (JDK 25+).** Un chemin de démarrage JVM pré-chauffé.

## Et ensuite

- [Why Nucleus](/docs/why-nucleus) — comment Nucleus se compare à Tauri et Electron.
- [Quickstart](/docs/start/quickstart) — construisez et lancez votre première app de bureau Nucleus.
- [Architecture](/docs/concepts/architecture) — le modèle en couches derrière le framework.
- [Runtimes](/docs/concepts/runtimes) — GraalVM Native Image et JVM + cache AOT.
- [Lecteur PDF](/docs/pdf) — affichez des PDF et extrayez du texte avec une seule API Compose sur Android, iOS, web et desktop.
