---
title: Project setup
description: Configure les dépôts, le plugin Nucleus, les modules runtime, une toolchain JDK et un layout Gradle multi-module.
---

import { Callout } from 'fumadocs-ui/components/callout';

Dans ce tutoriel, tu vas mettre en place un projet Nucleus multi-module. Tu vas configurer les
dépôts, appliquer le plugin Gradle, fixer les versions des modules runtime, choisir une
toolchain JDK, et organiser les modules `:app`, `:ui` et `:domain` pour que les dépendances
aillent dans un seul sens.

## Avant de commencer

Ce tutoriel prolonge le [Quickstart](/docs/start/quickstart), qui tient dans un seul module.
Tu as besoin de :

- **JDK 17 ou plus** pour un build standard, ou **JDK 25** pour activer le cache AOT.
- **Kotlin 2.0 ou plus** et **Gradle 8.x**.

## Configurer les dépôts

Résous le plugin depuis le Gradle Plugin Portal et les modules runtime depuis Maven Central.
Ajoute les deux dans `settings.gradle.kts` :

```kotlin title="settings.gradle.kts"
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        google()
    }
}
```

## Appliquer le plugin

Applique le plugin Nucleus dans le module qui contient ta fonction `main` :

```kotlin title="app/build.gradle.kts"
plugins {
    kotlin("jvm") version "2.4.0"
    id("org.jetbrains.compose") version "1.11.1"
    id("dev.nucleusframework") version "2.0.0"
}
```

<Callout type="tip">
Nucleus étend le plugin Compose au lieu de le remplacer, donc Compose Hot Reload fonctionne
sans configuration supplémentaire. Le `mainClass` que tu définis dans `nucleus.application`
est propagé à `compose.desktop.application.mainClass`, donc la tâche `hotRun` résout le point
d'entrée.
</Callout>

## Ajouter les modules runtime

Tous les artefacts runtime `dev.nucleusframework:nucleus.*` publient la même version. Fixe-la
une seule fois, puis déclare uniquement les modules que tu utilises :

```kotlin title="app/build.gradle.kts"
val nucleusVersion = "2.0.0"

dependencies {
    implementation(compose.desktop.currentOs)
    implementation("dev.nucleusframework:nucleus.nucleus-application:$nucleusVersion")
    implementation("dev.nucleusframework:nucleus.decorated-window-tao:$nucleusVersion")
    // N'ajoute que les modules dont tu as besoin :
    implementation("dev.nucleusframework:nucleus.notification-common:$nucleusVersion")
    implementation("dev.nucleusframework:nucleus.global-hotkey:$nucleusVersion")
}
```

`nucleus-application` est le module du point d'entrée. Il tire transitivement le core runtime,
l'AOT runtime et le GraalVM runtime. Ajoute `decorated-window-tao` pour le backend de fenêtre ;
tous les autres modules sont optionnels.

## Définir la toolchain JDK

Fixe une toolchain JDK pour que le build utilise une version de Java cohérente :

```kotlin title="app/build.gradle.kts"
kotlin {
    jvmToolchain {
        languageVersion.set(JavaLanguageVersion.of(25))
        // Optionnel : fixe le vendor pour des builds reproductibles
        // vendor.set(JvmVendorSpec.JETBRAINS)
    }
}
```

Utilise JDK 17 si tu n'as pas besoin du cache AOT. Utilise JDK 25 pour l'activer avec
`enableAotCache = true` dans `nativeDistributions`. Pour les native images GraalVM, définis la
version de Java avec `nucleus.application.graalvm { javaLanguageVersion.set(25) }`. Voir
[Configuration](/docs/start/configuration) pour les deux.

## Configurer gradle.properties

Ces réglages accélèrent le build et activent le configuration cache, que le plugin Nucleus
prend en charge :

```properties title="gradle.properties"
org.gradle.jvmargs=-Xmx4g -XX:+UseG1GC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

kotlin.code.style=official
```

## Organiser les modules

Un seul module suffit pour les petites applications. À mesure que l'application grandit, sépare
l'UI, le câblage applicatif et la logique métier dans des modules distincts :

```
my-app/
├── settings.gradle.kts
├── build.gradle.kts             ← conventions partagées, sans plugins
├── app/                         ← point d'entrée Nucleus + main()
│   └── build.gradle.kts         ← applique dev.nucleusframework
├── ui/                          ← composables Compose, thèmes
│   └── build.gradle.kts
└── domain/                      ← Kotlin pur : modèles, repositories, services
    └── build.gradle.kts
```

Suis ces règles pour garder les dépendances dans un seul sens :

- Seul `:app` applique `dev.nucleusframework`, et seul `:app` déclare
  `nucleus.application { … }`.
- `:domain` reste du Kotlin pur, ce qui te permet de le partager avec Android ou iOS plus tard
  en le déplaçant dans un source set `commonMain` Kotlin Multiplatform.
- Le code spécifique à une plateforme, comme un menu du dock macOS ou une jump list Windows,
  vit à côté du code qui l'utilise, le plus souvent dans `:app`.

## Partager du code entre plateformes

Les modules runtime de Nucleus sont JVM-only, mais `:domain` et `:ui` peuvent être des modules
Kotlin Multiplatform. Le module `:app` consomme leur target JVM et ne change pas quand tu
ajoutes d'autres targets.

## Et ensuite

- [Quickstart](/docs/start/quickstart) — construis et lance d'abord une application à module
  unique.
- [Configuration](/docs/start/configuration) — le DSL Gradle `nucleus { }` complet.
- [Modules](/docs/concepts/modules) — comment les modules Nucleus se composent.
- [Runtimes](/docs/concepts/runtimes) — les runtimes core, AOT et GraalVM.
