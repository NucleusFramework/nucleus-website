{
  "title": "Performance & native",
  "description": "Two runtimes from one build: a GraalVM native image or a JDK image with an AOT cache, plus the startup pack, native library loading, and OS trust for HTTP.",
  "pages": [
    "index",
    "startup",
    "benchmarks",
    "graalvm",
    "aot-cache",
    "native-code",
    "native-library-loader",
    "native-http",
    "native-ssl"
  ]
}
