{
  "title": "Performance & native",
  "description": "Two runtimes, one binary. GraalVM Native Image for instant cold start, JDK 25 with AOT cache for peak throughput — plus the native HTTP and SSL plumbing that makes them feel native.",
  "pages": ["index", "graalvm", "aot-cache", "native-access", "native-http", "native-ssl"]
}
